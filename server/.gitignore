# NestJS API — extends root .gitignore

# Compiled output
dist/

# Prisma local DB scratch files (migrations stay tracked)
*.db
*.db-journal

# User-uploaded catalog & deposit photos (runtime only)
uploads/*
!uploads/.gitkeep

# Local scripts output / one-off exports
exports/
tmp/

# Keep example env committable
!.env.example
