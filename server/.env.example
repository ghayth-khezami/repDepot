# Required — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

DATABASE_URL=postgresql://user:password@localhost:5432/depot

# Comma-separated allowed browser origins (HTTPS in production)
WEB_URL=http://localhost:3001
CLIENT_URL=http://localhost:5170
MOBILE_URL=http://localhost:5171

# Docker Compose only (docker-compose.prod.yml)
# POSTGRES_PASSWORD=change-me-strong

GOOGLE_CLIENT_ID=

# Web Push (optional — phone notifications when PWA is closed)
# Generate: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@bebe-depot.com

# Cloudinary — stockage images (gratuit, permanent, CDN)
# Dashboard: https://console.cloudinary.com → API Keys
# Or single line from Cloudinary dashboard:
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUDINARY_CLOUD_NAME
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
# Dossier racine dans Cloudinary (optionnel)
CLOUDINARY_FOLDER=bebe-depot

PORT=3000
NODE_ENV=development
