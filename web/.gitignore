# Next.js storefront — extends root .gitignore

# Next.js
.next/
out/

# Vercel
.vercel

# TypeScript (generated; safe to regenerate)
next-env.d.ts

# Yarn v2+ (if used)
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Keep example env committable
!.env.example
